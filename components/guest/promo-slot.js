/**
 * Height of the banner slot at the top of the listing left rail.
 *
 * The refurbished promo (products listing) and the rotating ads (refurbished
 * listing) sit in the same position on their respective pages, so they share one
 * height to keep the two rails looking identical. Change it here, not in the
 * components.
 */
export const PROMO_SLOT_HEIGHT = "h-60";
